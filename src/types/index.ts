/**
 * Shared domain types. Expanded across the roadmap; kept minimal for Part 1.
 */

/** Supported launch currencies (India-first, US-ready — STRATEGY §5). */
export type Currency = "INR" | "USD";

/** Garment families offered in the catalog (populated with data in Part 4). */
export type GarmentType = "tee" | "hoodie" | "oversized";
